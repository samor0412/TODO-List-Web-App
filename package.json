{
  "name": "todo-list-web-app",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "npm run -w backend test && npm run -w frontend test",
    "lint": "npm run -w backend lint && npm run -w frontend lint",
    "lint:fix": "npm run -w backend lint:fix && npm run -w frontend lint:fix"
  }
}
